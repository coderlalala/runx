name: sequential-chain
kind: graph
target: ../chains/sequential/chain.yaml
expect:
  status: success
  receipt:
    kind: graph_execution
    status: success
    graph_name: sequential-echo
  steps:
    - first
    - second
